const connection = require('../db/connection');
const depComms = require('../lib/depComms');

describe('General DB tests', () => {
    // HP
    describe('Gets tables', () => {
        let result = false
        expect(result).toBe(true);
    });
});