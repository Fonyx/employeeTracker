const connection = require('../db/connection');
const empComms = require('../lib/empComms');

describe('Employee tests', () => {
    // HP
    describe('view all tables'), () => {
        //HP
        it('should return valid entries for view all results', () => {
            let result = false
            expect(result).toBe(true);
        });
    }
});